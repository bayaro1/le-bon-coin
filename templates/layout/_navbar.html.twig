<nav class="navbar navbar-expand-lg bg-light" style="box-shadow: 0px 0px 5px grey;">
    <div class="container">
        <div class="container-fluid">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a style="color: orange; font-weight: bold; font-size: 1.5em; text-decoration: none;" aria-current="page" href="{{ path('home_index') }}">lebongroin</a>
                    </li>
                    <li class="nav-item ms-4">
                        <a class="btn" style="background-color: orange; color: white; font-weight: bold;" href="{{ path('product_new') }}"><i class="bi bi-plus-square"></i> Déposer une annonce</a>
                    </li>
                    <li class="nav-item ms-4">
                        <a class="nav-link {% if current_menu is defined and current_menu is same as 'product_view' %}underlined{% endif %}" href="{{ path('product_index') }}"><i class="bi bi-search"></i> Rechercher</a>
                    </li>
                </ul>
                <ul class="navbar-nav me-auto">
                    <li class="nav-item" style="text-align: center;">
                        <a class="nav-link" href="#"><i class="bi bi-bell"></i><br>Mes recherches</a>
                    </li>
                    <li class="nav-item" style="text-align: center;">
                        <a class="nav-link" {% if current_menu is defined and current_menu is same as 'cart' %}style="border-bottom: solid orange;"{% endif %} href="{{ path('cart_index') }}"> <i class="bi bi-heart"></i><br>Favoris</a>
                    </li>
                    <li class="nav-item" style="text-align: center;">
                        
                        <a class="nav-link" {% if current_menu is defined and current_menu is same as 'message' %}style="border-bottom: solid orange;"{% endif %} href="{{ path('message_index') }}"><i class="bi bi-chat-right-text"></i>
                            {% if conversation_repository.countNewByUser(app.user) > 0 %} <strong style="color: orange;">({{ conversation_repository.countNewByUser(app.user) }})</strong> {% endif %}
                            <br>Messages</a>
                    </li>
                    <li class="nav-item" style="text-align: center;">
                        {% if app.user %}
                            <a class="nav-link" {% if current_menu is defined and current_menu is same as 'account' %}style="border-bottom: solid orange;"{% endif %} href="{{ path('account_home_index') }}"><i class="bi bi-person-fill"></i><br>{{ app.user.username }}</a>
                        {% else %}
                            <a class="nav-link {% if current_menu is defined and current_menu is same as 'login' %}underlined{% endif %}" href="{{ path('security_login') }}"><i class="bi bi-person"></i><br>Se connecter</a>
                        {% endif %}
                    </li>
                </ul>
            </div>
        </div>
    </div>
</nav>

